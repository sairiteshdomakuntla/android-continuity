name: bridge_core
description: Native bridge platform channels
version: 0.0.1
environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

flutter:
  plugin:
    platforms:
      android:
        package: com.example.bridge_core
        pluginClass: BridgeCorePlugin
