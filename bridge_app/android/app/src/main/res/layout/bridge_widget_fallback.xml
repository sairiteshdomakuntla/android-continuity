<?xml version="1.0" encoding="utf-8"?>
<!-- Minimal fallback: pushed only if the full widget layout cannot be
     built. Guarantees the host never shows a black/empty widget. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_fallback_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/widget_bg"
    android:padding="16dp"
    android:gravity="center_vertical">

    <ImageView
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:background="@drawable/widget_dot" />

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:text="Bridge — tap to open"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="#0F172A" />
</LinearLayout>
